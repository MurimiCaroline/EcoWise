import 'package:flutter/material.dart';

class HostelName extends StatelessWidget {
  const HostelName({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hostel A and B"),
    );
  }
}